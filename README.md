# diz-web

Стартовый фронтенд-проект (Vite) с деплоем на Vercel.

## Локально

```bash
npm install
npm run dev
```

## Деплой

Пуш в `main` → Vercel собирает и публикует автоматически (после привязки репозитория).
